<VirtualHost *:80>
	ServerName {{ item.year }}.foss4g.be
    RewriteEngine on
    RewriteCond %{SERVER_NAME} ={{ item.year }}.foss4g.be
    RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>

